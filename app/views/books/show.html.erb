<% if @book.errors.any? %>
<% else %>
    <p>Book was successfully created.</p>
<% end %>
<P>
    <strong>Title:</strong>
    <%= @book.title %>
</P>
<P>
    <strong>Body:</strong>
    <%= @book.body %>
</p>

<%= link_to 'Edit', edit_book_path(@book.id) %>
<span>|</span>
<%= link_to 'Back', books_path %>